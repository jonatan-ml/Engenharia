<% @ad.comments.each do |comment| %> <%#= pega os comentários do ad %>
    <div class="row">
        <div class="col-md-12">
            <% if (current_member) && (comment.member.id == current_member.id) %>
                <%= rating_for_user @ad, current_member, 'quality' %>
            <% end %>

            <p><%= comment.body %></p>

            <p id="time-ago"><strong><%= comment.member.email %></strong></p>
            <p><strong><%= time_ago_in_words(comment.created_at) %></strong></p> <%#=tempo da postagem  %>
        </div>
    </div>

    <hr>
<% end %>